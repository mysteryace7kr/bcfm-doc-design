# 인쇄문서 디자인 시스템

부산공동어시장 재무기획팀의 **인쇄용 문서** 디자인 시스템. 화면이 아니라 종이를 전제로 한다 — 흰 종이, 검은 잉크, 링크 외 유채색 없음. 개조식 기호(`○ ▶ ※ ⇨`)의 위계를 내어쓰기로 정렬하는 것이 이 시스템의 뼈대다.

**세 서식 모두 자리표시(placeholder)만 들어 있는 뼈대다.** 마크다운 원고를 이 골격에 부어 문서를 만든다.

## 어느 템플릿을 쓸까

| 템플릿 | 쓸 때 | 기본 |
|---|---|---|
| **Report** | 여러 쪽, `○▶※⇨` 개조식 위계 + 요약밴드 + 번호 섹션 — 보고서·검토·개념설명·언론분석 | 1쪽 |
| **Formal** | 수신처·발신명의가 필요한 문서 — 기안(공문)·건의서·질의서·신청서. 결재란 포함 | 1쪽 |
| **Brief** | 1~2쪽, 타임라인·항목 나열 — 업무진행사항·요약보고 | 1쪽 |

애매하면 **Report**.

## 지면 — 쪽은 브라우저가 나눈다

세 서식은 **흐름 문서**다. 원고를 `<doc-page size="a4" margin="20mm">` 안에
이어 쓰고 푸터는 `slot="footer"`로 한 번만 둔다. `section.page`는 만들지 않는다.
화면은 A4 폭의 긴 시트이며 쪽 경계는 실제 PDF에서 확인한다.

템플릿의 `@page { size:A4 }`는 BCFM의 A4 출력 계약이다. 런타임의 여백 설정과
중복되지 않도록 `@page`에 여백을 추가하지 않는다. 브라우저 대화상자에서도 A4,
배율 100%, 머리글/바닥글 해제를 확인한다. 다른 용지로 재정의하면 재검증한다.

기본 상하좌우 여백은 20mm, 가로 본문 폭은 170mm(약 642.5px)다.
기본 세로 공간은 257mm(약 971.3px)이나 머리글·푸터가 커지면 더 줄어든다.
19/20/14mm와 `.page` 기반 용량표는 이전 고정 쪽 방식의 값이다.

### 줄바꿈과 쪽 나눔

- 공통 `keep-all`은 한글 단어 중간 분리를 줄인다. `overflow-wrap:anywhere`는
  한 줄보다 긴 URL·식별자 등이 전체 지면 폭을 늘리는 것을 막는 예외다.
- `pretty`는 짧은 마지막 줄을 줄이려는 브라우저의 최적화이며 보장이 아니다.
  `widows/orphans:2`는 같은 문단의 페이지 경계 줄 수를 다룬다. 한 단어 고립이나
  다음 쪽의 독립된 한 줄 문단까지 제거하지 않는다.
- 제목은 뒤 내용과 붙인다. 문단·긴 인용은 흐르게 하고 표는 행 단위로 나눈다.
  긴 표에는 `thead`를 둔다. 카드·짧은 밴드·결재·서명만 통째로 유지한다.
- 긴 카드에는 `allow-split`, 짧고 의미상 붙여야 하는 단위에는 `keep-phrase`를 쓴다.
  문장 전체의 `nowrap`, 강제 `<br>`, 전체 표의 분할 금지를 기본으로 넣지 않는다.
- `break-before:page`는 명시적인 새 쪽 요구에만 사용한다.

### 검증과 마지막 한 줄

`checkpdf.sh`는 **본문 잉크 도달률 참고 검사**다. 기본 A4·20mm 여백만 가정하며,
92%는 검토 신호다. 제목·표 행이 함께 이동하면 그보다 덜 차도 정상일 수 있다.
채움률로 원문 보존·폰트·잘림·고립 줄의 종합 합격을 선언하지 않는다.
완료 전 PDF를 열어 모든 쪽을 확인한다.

한 줄 때문에 쪽이 늘면 먼저 강제 줄바꿈·큰 문단 여백·유지 블록을 확인한다.
그 뒤 실제 PDF를 비교하며 간격을 소폭 조정하고, 필요하면 줄간과 본문 크기를
속성 패널 범위에서 조정한다. 본문 13px 미만으로 줄이지 않는다.
자동 보정기는 아직 없으며 `fit.sh`는 **이전 고정 쪽 문서 전용**이다.
흐름 문서에 사용하지 않는다. 조건을 지킬 수 없으면 추가 쪽을 허용한다.
원문을 삭제하거나 채움률을 위해 내용을 지어내지 않는다.

쪽 번호는 현 런타임 푸터에 구현돼 있지 않다. 'Chrome 미지원' 때문은 아니다.
Chrome 131+의 `@page` 여백상자와 `page/pages` 카운터는 가능하지만,
현재의 여백 0·고정 푸터 방식을 함께 재설계해야 한다.

```bash
./sync/checkpdf.sh 내문서.dc.html
```

## 속성 패널

| 템플릿 | 본문 | 줄간 |
|---|---|---|
| Report · Brief | 14px (12.5~15.5) | 1.85 (1.70~2.00) |
| Formal | 13.5px (12.0~15.0) | 1.75 (1.60~1.90) |

공문은 한 쪽에 맞추는 것이 관행이라 Formal을 더 촘촘하게 잡았다.
**한 쪽에서 조금 넘칠 때는 이 슬라이더를 먼저 낮춰본다.** 그래도 안 되면 쪽을 나눈다.

## 색

종이는 흰색. **링크 외 컬러 액센트 금지.**

| 토큰 | 값 | 용도 |
|---|---|---|
| `--ink` | `#1c1d20` | 본문 |
| `--ink-strong` | `#141619` | 제목·`⇨` 결과·`<strong>` |
| `--ink-mid` | `#2a2b2e` | 표 라벨·인용문·`▶` 부연 |
| `--ink-soft` | `#63656a` | 보조 텍스트 |
| `--muted` | `#8b8880` | 푸터·용도표기·`※` 참고 |
| `--band` | `#f6f4ef` | 요약밴드·표 라벨열 |
| `--rule` | `#dcd7cd` | 표 테두리 |
| `--rule-soft` | `#e2ddd3` | 밴드 상하선 |
| `--rule-strong` | `#cec9be` | h2 밑줄 |
| `--rule-hair` | `#ddd8ce` | 푸터 라인 |
| `--link` / `--link-hover` | `#2c4a6e` / `#16324f` | 링크 (유일한 유채색) |

## 서체

- **`Noto Sans KR` 하나로 통일.** 제목도 고딕이다 — Serif는 쓰지 않는다.
  (`fonts/` 의 Serif TTF는 보존만 되어 있고 참조하지 않는다.)
- 자간: 본문 `-0.015em`, h1 `-0.03em`, h2·h3 `-0.02em`
- 정렬: `text-align:justify` + `word-break:keep-all` (한글 단어 보호, 긴 토큰에는 예외 줄바꿈)
- 본문 `--body-fs` / 줄간 `--body-lh`는 `<style>` 맨 위에서 `@property`로 등록돼 있다.
  등록하지 않으면, 속성 패널 바인딩이 해결되지 않은 렌더 경로에서 `--body-fs: px` 같은
  **무효값**이 들어가는데 — 이때 `var(--body-fs, 14px)`의 폴백은 발동하지 않는다
  (변수가 *없는* 게 아니라 *무효*라서). 계산 시점에 무효 처리되어 상속값인
  **16px / line-height:normal** 로 튄다. `@property`의 `initial-value`가 이때
  본래 기본값으로 되돌려 준다. **이 블록을 지우지 않는다.**

## 표제 위계

| 요소 | 사양 |
|---|---|
| `.kicker` 용도표기 | 10.5px / 500 / 자간 0.34em / `--muted` / 가운데 |
| `h1` | 25px / 700 / 줄간 1.34 / 가운데 |
| `.rule86` | 제목 아래 폭 86px, 2px 실선 `--ink-strong` |
| `h2` | 17px / 700 / 전폭 밑줄 1px `--rule-strong` / 위 26px |
| `h3` | 15px / 700 / 위 20px |

## 개조식 기호 위계

내어쓰기(hanging indent)로 기호와 본문을 정렬한다.

| 클래스 | 기호 | 들여쓰기 | 크기 | 색 |
|---|---|---|---|---|
| `.o` | `○` 설명내용 | 1.15em | 본문 | `--ink` |
| `.k` | `가. 나. 다.` 하위항목 | 2.45em | 본문 | `--ink` |
| `.t` | `▶` 부연 | 2.45em | 0.94배 | `--ink-mid` |
| `.n` | `※` 참고·출처 | 2.45em | 0.88배 | `--muted` |
| `.r` | `⇨` 결과·도출 | 1.3em | 본문 / **700** | `--ink-strong` |
| `.lead` | 번호 문단 (기안 본문) | 없음 | 본문 | `--ink` |

`▢`는 쓰지 않는다 (웹폰트에 글리프가 없어 `□`로 폴백된다).

개조식 문단·`.lead`·`.quote`의 `orphans`/`widows`는 **2**다.

## 블록

- `.band` 요약밴드 — 전폭, `--band` 배경, 상하선만, 좌우 테두리 없음
- `.quote` 인용·내부메모 — 밴드와 같은 틀, 0.92배, `--ink-mid`
- `table` — 라벨 열 `th`에 `--band` 음영, 실선 격자 0.8px `--rule`, 수치는 `td.num`으로 우측정렬(텍스트에는 쓰지 않는다), `thead th`는 가운데
- `.foot` 푸터 — **`slot="footer"`에 한 번 넣는다.** 좌측 문서명 / 우측 기관명, 9.5px, 상단 헤어라인

## 고칠 때 지킬 것

- BCFM 템플릿의 `@page { size:A4 }`를 유지하고 여백은 `doc-page margin="20mm"`에만 둔다.
- `size="a4"`, `@property` 등록, `doc-page`의 글꼴 지정을 유지한다.
- 푸터는 `slot="footer"`로 두고 고정 쪽 구조와 혼용하지 않는다.
- 링크 외 유채색을 추가하지 않는다. 색·크기는 기존 토큰을 사용한다.
- `runtime/`은 외부 런타임이다. 변경 대신 템플릿 쪽 규칙을 조정한다.

## 파일

- `sync/checkpdf.sh` — **A4로 뽑아 기본 여백 안 본문 잉크 도달률을 참고값으로 출력한다**
- `sync/topdf.sh` · `sync/fontcache.sh` — 확인용 PDF · 측정용 글꼴 내려받기
- `sync/fit.sh` · `sync/measure.sh` — 고정 쪽(`section.page`) 문서 전용. 이 서식에는 쓰지 않는다
- `styles.css` — 토큰 시트. 각 `.dc.html`의 `:root`와 같은 값이며, 값을 바꿀 때 두 곳을 함께 고친다.
- `templates/report.dc.html` — 보고서·검토 뼈대 (1쪽)
- `templates/formal.dc.html` — 기안(공문)·건의서·질의서 뼈대 (1쪽)
- `templates/brief.dc.html` — 업무진행·요약보고 뼈대 (1쪽)
- `runtime/`의 `doc-page.js`·`support.js` (업로드본은 각 템플릿 폴더로 복제) — 지면 분할 런타임

## 관련 스킬

- `doc-design` — 이 디자인 시스템 (설치 위치 `~/.claude/skills/doc-design/`)
- `doc-style` — 어법·종결어미·문서 골격. 8절이 이 시각 디자인의 출처다.
