# 인쇄문서 디자인 시스템

부산공동어시장 재무기획팀의 **인쇄용 문서** 디자인 시스템. 화면이 아니라 종이를 전제로 한다 — 흰 종이, 검은 잉크, 링크 외 유채색 없음. 개조식 기호(`○ ▶ ※ ⇨`)의 위계를 내어쓰기로 정렬하는 것이 이 시스템의 뼈대다.

**세 서식 모두 자리표시(placeholder)만 들어 있는 뼈대다.** 마크다운 원고를 이 골격에 부어 문서를 만든다.

## 어느 템플릿을 쓸까

| 템플릿 | 쓸 때 | 기본 |
|---|---|---|
| **Report** | 여러 쪽, `○▶※⇨` 개조식 위계 + 요약밴드 + 번호 섹션 — 보고서·검토·개념설명·언론분석 | 1쪽 |
| **Formal** | 수신처·발신명의가 필요한 문서 — 기안(공문)·건의서·질의서·신청서. 결재란 포함 | 1쪽 |
| **Brief** | 1~2쪽, 타임라인·항목 나열 — 업무진행사항·요약보고 | 1쪽 |

애매하면 **Report**.

## 지면 — 이 시스템의 핵심

`<doc-page size="a4">` 안에 **`<section class="page">` 하나가 A4 한 쪽**이다.

`<section class="page">` 하나 = 편집 화면 카드 하나 = PDF 한 쪽 = A4 용지 한 장.
넷이 **항상 1:1**이다. 양면 인쇄를 전제하지 않으므로 종이 장수와 쪽수가 같다.
혼동을 막기 위해 **"장"·"페이지"를 쓰지 않고 "쪽"으로만 센다.** 푸터의 `n / N`이 그 번호다.

```html
<doc-page size="a4" margin="0">
  <section class="page">
    <div class="body"> … 내용 … </div>
    <div class="foot"><span>문서명</span><span>1 / 2</span></div>
  </section>
  <section class="page"> … 2쪽 … </section>   <!-- 쪽을 늘리려면 이렇게 -->
</doc-page>
```

이 구조가 주는 것:

- **편집 화면에 한 쪽이 카드 하나로 보인다.** 화면에서 본 그대로 인쇄된다.
- **`@page`에 A4가 못박힌다** (`size: 210mm 297mm`). 내보내기 대화상자 용지에 휘둘리지 않는다.
- **쪽 번호를 쓸 수 있다.** 푸터에 직접 적는다.

### 쪽을 늘리려면

`<section class="page">` 블록을 통째로 복사해 붙이고, 푸터의 `n / N`만 고친다.
**쪽수 제한은 없다.** section 이 10개면 그대로 10쪽 나온다.

### 넘치면 잘린다 — 대신 화면에 보인다

한 쪽에 내용이 넘치면 다음 쪽으로 흘러가지 않고 **잘린다**(`overflow:hidden`).
내용을 줄이라는 뜻이 아니라 **쪽을 하나 더 만들라는 뜻**이다.

잘렸는지는 **편집 화면 카드에서 그대로 보인다.** 카드가 곧 인쇄될 종이다.
아래쪽이 잘려 보이면 그게 PDF에서도 잘린 것이니, section을 하나 더 만들어 나눈다.

### 한 쪽에 들어가는 분량

A4 297mm에서 위 19mm·아래 14mm 여백과 푸터(약 6mm)를 뺀 **본문 영역이 973.7px = 257.6mm**다.
여기에 몇 줄이 들어가는지는 속성 패널 값에 따라 달라진다.

| 서식 | 본문 / 줄간 | 줄높이 | 한 쪽 용량 | 95% 목표 |
|---|---|---|---|---|
| Report·Brief | **14px / 1.85** (기본) | 25.9px | **37.6줄** | **35.7줄** |
| Report·Brief | 12.5px / 1.70 (최소) | 21.3px | 45.8줄 | 43.5줄 |
| Report·Brief | 15.5px / 2.00 (최대) | 31.0px | 31.4줄 | 29.8줄 |
| Formal | **13.5px / 1.75** (기본) | 23.6px | **41.2줄** | **39.2줄** |
| Formal | 12.0px / 1.60 (최소) | 19.2px | 50.7줄 | 48.2줄 |
| Formal | 15.0px / 1.90 (최대) | 28.5px | 34.2줄 | 32.5줄 |

여기서 **"줄"은 본문 한 줄 높이로 환산한 값**이다. h2 위아래 여백·요약밴드·표는
본문보다 자리를 더 먹으므로, 눈에 보이는 글자 줄을 세는 것이 아니다.

### 채움률 판정

| 채움률 | 판정 | 할 일 |
|---|---|---|
| 100% 넘음 | **잘린다** | 쪽을 나누거나 슬라이더를 낮춘다 |
| 92~100% | 적정 | 그대로 둔다 |
| 80~92% | 조금 빈다 | 줄간을 올리거나 내용을 보탠다 |
| 80% 미만 | 비었다 | 뒤 쪽에서 당겨오거나 쪽을 합친다 |

목표는 **95% 안팎**이다.

**뼈대는 이 기준에서 뺀다.** `templates/` 의 세 서식은 자리표시만 든 출발점이라
채우려고 가짜 항목을 넣을 이유가 없다. 기준은 **원고를 부어 완성한 문서**에
적용한다. (참고로 지금 뼈대는 report 93.9% / formal 82.8% / brief 64.5%다.)

**이건 자동으로 맞춰지지 않는다.** 지면은 고정된 A4 상자이고 `.body{flex:1}`이
푸터를 바닥으로 밀 뿐, 내용을 늘리거나 줄이는 로직은 어디에도 없다. 남는 공간은
그대로 빈 여백이 된다. 원고 분량으로 맞추는 수밖에 없고, 맞았는지는 재봐야 안다.

```bash
./sync/measure.sh 내문서.dc.html   # 쪽별 채움률을 잰다
```

## 속성 패널

| 템플릿 | 본문 | 줄간 |
|---|---|---|
| Report · Brief | 14px (12.5~15.5) | 1.85 (1.70~2.00) |
| Formal | 13.5px (12.0~15.0) | 1.75 (1.60~1.90) |

공문은 한 쪽에 맞추는 것이 관행이라 Formal을 더 촘촘하게 잡았다.
**한 쪽에서 조금 넘칠 때는 이 슬라이더를 먼저 낮춰본다.** 그래도 안 되면 쪽을 나눈다.

## 색

종이는 흰색. **링크 외 컬러 액센트 금지.**

| 토큰 | 값 | 용도 |
|---|---|---|
| `--ink` | `#1c1d20` | 본문 |
| `--ink-strong` | `#141619` | 제목·`⇨` 결과·`<strong>` |
| `--ink-mid` | `#2a2b2e` | 표 라벨·인용문·`▶` 부연 |
| `--ink-soft` | `#63656a` | 보조 텍스트 |
| `--muted` | `#8b8880` | 푸터·용도표기·`※` 참고 |
| `--band` | `#f6f4ef` | 요약밴드·표 라벨열 |
| `--rule` | `#dcd7cd` | 표 테두리 |
| `--rule-soft` | `#e2ddd3` | 밴드 상하선 |
| `--rule-strong` | `#cec9be` | h2 밑줄 |
| `--rule-hair` | `#ddd8ce` | 푸터 라인 |
| `--link` / `--link-hover` | `#2c4a6e` / `#16324f` | 링크 (유일한 유채색) |

## 서체

- **`Noto Sans KR` 하나로 통일.** 제목도 고딕이다 — Serif는 쓰지 않는다.
  (`fonts/` 의 Serif TTF는 보존만 되어 있고 참조하지 않는다.)
- 자간: 본문 `-0.015em`, h1 `-0.03em`, h2·h3 `-0.02em`
- 정렬: `text-align:justify` + `word-break:keep-all` (한 글자 넘김 방지)
- 본문 `--body-fs` / 줄간 `--body-lh`는 `<style>` 맨 위에서 `@property`로 등록돼 있다.
  등록하지 않으면, 속성 패널 바인딩이 해결되지 않은 렌더 경로에서 `--body-fs: px` 같은
  **무효값**이 들어가는데 — 이때 `var(--body-fs, 14px)`의 폴백은 발동하지 않는다
  (변수가 *없는* 게 아니라 *무효*라서). 계산 시점에 무효 처리되어 상속값인
  **16px / line-height:normal** 로 튄다. `@property`의 `initial-value`가 이때
  본래 기본값으로 되돌려 준다. **이 블록을 지우지 않는다.**

## 표제 위계

| 요소 | 사양 |
|---|---|
| `.kicker` 용도표기 | 10.5px / 500 / 자간 0.34em / `--muted` / 가운데 |
| `h1` | 25px / 700 / 줄간 1.34 / 가운데 |
| `.rule86` | 제목 아래 폭 86px, 2px 실선 `--ink-strong` |
| `h2` | 17px / 700 / 전폭 밑줄 1px `--rule-strong` / 위 26px |
| `h3` | 15px / 700 / 위 20px |

## 개조식 기호 위계

내어쓰기(hanging indent)로 기호와 본문을 정렬한다.

| 클래스 | 기호 | 들여쓰기 | 크기 | 색 |
|---|---|---|---|---|
| `.o` | `○` 설명내용 | 1.15em | 본문 | `--ink` |
| `.k` | `가. 나. 다.` 하위항목 | 2.45em | 본문 | `--ink` |
| `.t` | `▶` 부연 | 2.45em | 0.94배 | `--ink-mid` |
| `.n` | `※` 참고·출처 | 2.45em | 0.88배 | `--muted` |
| `.r` | `⇨` 결과·도출 | 1.3em | 본문 / **700** | `--ink-strong` |
| `.lead` | 번호 문단 (기안 본문) | 없음 | 본문 | `--ink` |

`▢`는 쓰지 않는다 (웹폰트에 글리프가 없어 `□`로 폴백된다).

개조식 문단·`.lead`·`.quote`의 `orphans`/`widows`는 **2**다.

## 블록

- `.band` 요약밴드 — 전폭, `--band` 배경, 상하선만, 좌우 테두리 없음
- `.quote` 인용·내부메모 — 밴드와 같은 틀, 0.92배, `--ink-mid`
- `table` — 라벨 열 `th`에 `--band` 음영, 실선 격자 0.8px `--rule`, 수치는 `td.num`으로 우측정렬(텍스트에는 쓰지 않는다), `thead th`는 가운데
- `.foot` 푸터 — **각 `section.page` 안에 직접 넣는다.** 좌측 문서명 / 우측 `n / N`, 9.5px, 상단 헤어라인

## 고칠 때 지킬 것

- `@page` 규칙을 직접 쓰지 않는다 — `doc-page.js`가 소유하며, `section.page` 구조에서는 알아서 `size: 210mm 297mm`를 넣어 준다
- **`size="a4"`를 지우지 않는다** — 이게 화면 카드 크기와 인쇄 용지를 동시에 정한다
- **`<style>` 맨 위의 `@property --body-fs` / `--body-lh` 블록을 지우지 않는다** — 「서체」 참조
- `.page`의 여백 `19mm 20mm 14mm`(상/좌우/하)를 임의로 바꾸지 않는다
- 푸터를 `slot="footer"`로 옮기지 않는다 — `section.page`와 혼용이 금지되어 있고, 옮기면 쪽 번호를 쓸 수 없다
- 링크 외 유채색을 넣지 않는다
- 색·크기를 인라인으로 새로 쓰지 말고 `var(--토큰)`을 쓴다

## 파일

- `styles.css` — 토큰 시트. 각 `.dc.html`의 `:root`와 같은 값이며, 값을 바꿀 때 두 곳을 함께 고친다.
- `templates/report/report.dc.html` — 보고서·검토 뼈대 (1쪽)
- `templates/formal/formal.dc.html` — 기안(공문)·건의서·질의서 뼈대 (1쪽)
- `templates/brief/brief.dc.html` — 업무진행·요약보고 뼈대 (1쪽)
- 각 템플릿 폴더의 `doc-page.js`·`support.js` — 지면 분할 런타임

## 관련 스킬

- `doc-design` — 이 디자인 시스템 (설치 위치 `~/.claude/skills/doc-design/`)
- `doc-style` — 어법·종결어미·문서 골격. 8절이 이 시각 디자인의 출처다.
