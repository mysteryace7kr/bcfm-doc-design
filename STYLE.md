# 인쇄문서 디자인 시스템

부산공동어시장 재무기획팀의 **인쇄용 문서** 디자인 시스템. 화면이 아니라 종이를 전제로 한다 — 흰 종이, 검은 잉크, 링크 외 유채색 없음. 개조식 기호(`○ ▶ ※ ⇨`)의 위계를 내어쓰기로 정렬하는 것이 이 시스템의 뼈대다.

**서식은 보고서 하나다.** 자리표시(placeholder)만 들어 있는 뼈대이며, 마크다운 원고를 이 골격에 부어 문서를 만든다. 검토보고서·개념설명·언론분석·기안·건의서·질의서·업무진행 보고를 모두 이 하나로 쓴다. 기안의 수신처·발신명의·결재란처럼 서식에 없는 요소는 그 문서에서 만들어 넣는다.

> 2026-09-10 에 formal(기안)·brief(업무진행) 두 서식을 폐기했다. 세 벌을 따로 손보다 보니 하나를 고칠 때마다 나머지 둘이 어긋났다. 지운 파일은 `notes/backup-26.9.10/` 에 있다.

## 지면 — `section.page` 한 장이 A4 한 장

**고정 쪽 문서다.** `<section class="page">` 하나가 A4 한 장이고, 이 시스템에서는 그 한 장을 **"쪽"** 이라고 부른다("장"·"페이지"를 섞어 쓰지 않는다).

```html
<doc-page size="a4" margin="0">
<section class="page">
 <div class="body">
  <div class="kicker">…</div>
  <h1>제목</h1>
  … 본문 …
 </div>
  <div class="foot"><span>문서명</span><span>1 / 2</span></div>
</section>
<section class="page">
 <div class="body">… 다음 쪽 …</div>
  <div class="foot"><span>문서명</span><span>2 / 2</span></div>
</section>
</doc-page>
```

이 방식이 주는 것 — **Claude Design 캔버스에 A4 종이가 한 장씩 카드로 뜬다.** 쪽 경계를 화면에서 그대로 보면서 편집하고, 쪽 번호를 푸터에 적을 수 있다.

대신 지켜야 하는 것 — **쪽 박스를 넘치는 내용은 잘린다.** 다음 쪽으로 흘러가지 않는다. 대신 잘린 자리가 화면에서 바로 보이므로, 그때 `section.page` 를 하나 늘려 이어 붓는다.

- **여백은 `.page` 의 padding 이 소유한다**(상 19mm / 좌우 20mm / 하 14mm). `<doc-page>` 의 `margin` 은 고정 쪽에서 무효라 `margin="0"` 으로 둔다.
- **푸터는 `.page` 안의 `.foot`** 이다. `slot="footer"` 를 쓰지 않는다 — 그건 흐름 문서용이고 고정 쪽과 혼용이 금지되어 있다.
- 흐름 문서 내용과 `section.page` 를 섞지 않는다. `break-before:page` 도 쓰지 않는다(쪽을 이미 손으로 나눴다).

### 쪽을 나누는 규칙

쪽은 **채워서** 넘긴다. 2026-09-07 에 지적받은 것들이다.

- **제목마다 쪽을 새로 시작하지 않는다.** `1.` `2.` `3.` 이 새 쪽에서 시작할 이유가 없다
- **「이어서」·「(계속)」 같은 제목을 만들지 않는다.** 넘어가면 그냥 넘어간 것이다
- **쪽을 일부러 비우지 않는다.** 특별히 요청받지 않는 한 채우고 넘어간다
- 마지막 쪽이 헐거운 건 정상이니 메우려고 말을 지어내지 않는다

### 용지 — `size="a4"` 하나가 전부다

`doc-page.js` 가 고정 쪽 문서에서는 `@page` 에 `size: 210mm 297mm` 를 직접 쓴다(`doc-page.js:559-565`). 그 규칙은 `<head>` 에 마지막으로 주입되므로 문서 안의 다른 `@page` 보다 우선한다.

- **`@page` 를 직접 쓰지 않는다.** 런타임 것이 이긴다. 2026-09-10 까지 서식에 있던 `@page { size: A4; }` 한 줄은 흐름 문서 시절의 우회였고, 고정 쪽에서는 필요 없으므로 지웠다.
- **`size="a4"` 를 지우지 않는다.** 이 속성 하나가 화면의 쪽 카드 크기와 인쇄 용지를 동시에 정한다. 지우면 Letter(A4보다 18mm 짧다)로 떨어진다.

실측(2026-09-10, 헤드리스 크롬에 아무것도 주입하지 않고 그대로 출력) — 보고서 뼈대가 **595×842pt = 210×297mm, 1쪽**, PDF 에 Pretendard 임베드됨.

### 분량을 맞추는 법

속성 패널의 **본문크기·줄간 슬라이더**가 유일한 손잡이다. 헐거워 보이면 한 눈금 낮추고, 빽빽하면 올린다. 본문 크기·줄간을 손으로 고치지 않는다.

### 한 쪽에 들어가는 분량

A4 297mm에서 위 19mm·아래 14mm 여백과 푸터(약 6mm)를 뺀 **본문 영역이 973.7px = 257.6mm**다.

| 본문 / 줄간 | 줄높이 | 한 쪽 용량 | 95% 목표 |
|---|---|---|---|
| **14px / 1.85** (기본) | 25.9px | **37.6줄** | **35.7줄** |
| 12.5px / 1.70 (최소) | 21.3px | 45.8줄 | 43.5줄 |
| 15.5px / 2.00 (최대) | 31.0px | 31.4줄 | 29.8줄 |

여기서 **"줄"은 본문 한 줄 높이로 환산한 값**이다. h2 위아래 여백·요약밴드·표는 본문보다 자리를 더 먹으므로, 눈에 보이는 글자 줄을 세는 것이 아니다.

### 합격 기준

`fit.sh` 는 마지막에 **합격 / 불합격**을 찍는다. 이게 이 시스템의 기준이다.

| 항목 | 기준 | 왜 |
|---|---|---|
| 잘린 쪽 | **0** | 내용이 사라진다 |
| 마지막 쪽을 뺀 모든 쪽 | **채움률 92% 이상** | 헐거운 쪽을 남기지 않는다 |
| 마지막 쪽 | **기준 없음** | 꼬리는 남는 내용만큼만 찬다. 메우려고 말을 지어내지 않는다 |
| 본문 크기 | **13px 이상** | 인쇄해서 사람이 읽는다 |
| 본문 글자 | **그대로** | 조판이 내용을 바꾸면 안 된다 |
| 외톨이 줄 | **0** | 「줄바꿈」 참조 |

**합격이면 그대로 낸다.** 눈으로 다시 맞추지 않는다.

**불합격이면 도구가 더 할 수 있는 게 없다는 뜻이다.** 까닭별로 할 일이 다르다.

| 불합격 사유 | 할 일 |
|---|---|
| 넘쳐서 잘린다 | 쪽을 하나 늘려 이어 붓는다. 한 블록이 한 쪽보다 크면 그 표를 둘로 나눈다 |
| 92% 못 채운 쪽이 있다 | 그 자리에 나눌 수 없는 표·제목 덩어리가 걸렸다. **그 쪽만 사람이 본다** |
| 본문이 13px 미만 | 원고가 길다. 줄이거나 쪽수를 늘린다 — 글자를 더 줄이지 않는다 |

기준을 못 지킨 채로 "그럴듯해 보인다"고 넘기지 않는다. 그렇게 넘긴 문서가 8쪽 평균 74% 로 나왔다.

### 채움률 판정

| 채움률 | 판정 | 할 일 |
|---|---|---|
| 100% 넘음 | **잘린다** | 쪽을 나누거나 슬라이더를 낮춘다 |
| 92~100% | 적정 | 그대로 둔다 |
| 80~92% | 조금 빈다 | 줄간을 올리거나 내용을 보탠다 |
| 80% 미만 | 비었다 | 뒤 쪽에서 당겨오거나 쪽을 합친다 |

목표는 **95% 안팎**이다.

**뼈대는 이 기준에서 뺀다.** `templates/report.dc.html` 은 자리표시만 든 출발점이라 채우려고 가짜 항목을 넣을 이유가 없다. 기준은 **원고를 부어 완성한 문서**에 적용한다. (참고로 지금 뼈대는 93.9%다.)

재는 법 — DOM 으로 쪽 경계를 알 수 있으므로 PDF 를 뽑지 않고 바로 잰다.

```bash
./sync/measure.sh 내문서.dc.html     # 쪽별 채움률
./sync/fit.sh     내문서.dc.html     # 채움을 맞추고 합격·불합격 판정
```

## 줄바꿈 — 한 단어·한 글자를 외톨이로 남기지 않는다

한글의 기본 줄바꿈은 **글자 단위**라 그냥 두면 어절 중간이 끊긴다(「판단된다」의 「고」만 다음 줄로 떨어지는 식). 세 가지를 서식이 함께 건다.

| 속성 | 하는 일 |
|---|---|
| `word-break: keep-all` | 어절 중간을 쪼개지 않는다 |
| `text-wrap: pretty` | 마지막 줄에 한 단어만 남으면 줄바꿈을 다시 계산한다 |
| `hanging-punctuation: allow-end` | 줄 끝 마침표·쉼표를 여백으로 흘려 한 글자가 넘어가는 것을 막는다 |

제목(`h1`·`h2`·`h3`)은 `text-wrap: balance` 로 단 수를 고르게 잡는다.

**`&nbsp;` 를 넣지 않는다.** 원고 글자를 건드리지 않고 서식만으로 잡는다.

검사는 실제 렌더에서 한다 — `Range` 로 글자마다 사각형을 읽어 줄을 세고, 두 줄 이상인 문단의 마지막 줄에 남은 글자를 본다.

```bash
./sync/orphan.sh 내문서.dc.html
```

판정은 마지막 줄이 **한 글자면 불합격**, **한 단어 3글자 이하면 주의**다.

실측(2026-09-10) — 어절을 하나씩 늘려가며 만든 27개 문단으로 A/B 대조한 결과, 규칙을 끄면 마지막 줄이 「함께.」·「다고.」 인 문단이 나오고(뒤엣것은 어절 중간이 끊긴 것), 켜면 **0건**이다.

## 속성 패널

| 항목 | 기본 | 범위 |
|---|---|---|
| 본문 | 14px | 12.5 ~ 15.5 |
| 줄간 | 1.85 | 1.70 ~ 2.00 |

**한 쪽에서 조금 넘칠 때는 이 슬라이더를 먼저 낮춰본다.** 그래도 안 되면 쪽을 나눈다.

## 색

종이는 흰색. **링크 외 컬러 액센트 금지.**

| 토큰 | 값 | 용도 |
|---|---|---|
| `--ink` | `#1c1d20` | 본문 |
| `--ink-strong` | `#141619` | 제목·`⇨` 결과·`<strong>` |
| `--ink-mid` | `#2a2b2e` | 표 라벨·인용문·`▶` 부연 |
| `--ink-soft` | `#63656a` | 보조 텍스트 |
| `--muted` | `#8b8880` | 푸터·용도표기·`※` 참고 |
| `--band` | `#f6f4ef` | 요약밴드·표 라벨열 |
| `--rule` | `#dcd7cd` | 표 테두리 |
| `--rule-soft` | `#e2ddd3` | 밴드 상하선 |
| `--rule-strong` | `#cec9be` | h2 밑줄 |
| `--rule-hair` | `#ddd8ce` | 푸터 라인 |
| `--link` / `--link-hover` | `#2c4a6e` / `#16324f` | 링크 (유일한 유채색) |

## 서체

- **`Pretendard` 하나로 통일.** 제목도 본문도 같은 글꼴이다 — Serif는 쓰지 않는다.
- 물리는 방식은 `@font-face` 하나뿐이다. **`local('Pretendard')` 가 먼저**라 맥에 설치돼 있으면 그것을 쓰고(네트워크도 다운로드도 없다), 없는 환경에서만 프로젝트 `fonts/Pretendard-*.ttf`(400·500·700) 로 떨어진다.
  Pretendard 는 Google Fonts 에 없어서 웹폰트 `<link>` 를 쓸 수 없다. 이 `@font-face` 가 유일한 경로다.
  경로는 Claude Design 배치 기준(`../../fonts/`)이며, 저장소본과 업로드본이 같은 줄을 쓴다 — 예전에 이 치환을 하던 `sync/fontswap.py` 는 원본과 업로드본이 달라지는 원인이어서 없앴다.
- `fonts/` 의 Noto TTF 8개는 Claude Design 앱 소유다. 참조하지 않고, 지우지도 않는다(맥에 원본이 없어 복구가 안 된다).
- 자간: 본문 `-0.015em`, h1 `-0.03em`, h2·h3 `-0.02em`
- 정렬: `text-align:justify` — 줄바꿈 규칙은 「줄바꿈」 참조
- 본문 `--body-fs` / 줄간 `--body-lh`는 `<style>` 맨 위에서 `@property`로 등록돼 있다.
  등록하지 않으면, 속성 패널 바인딩이 해결되지 않은 렌더 경로에서 `--body-fs: px` 같은
  **무효값**이 들어가는데 — 이때 `var(--body-fs, 14px)`의 폴백은 발동하지 않는다
  (변수가 *없는* 게 아니라 *무효*라서). 계산 시점에 무효 처리되어 상속값인
  **16px / line-height:normal** 로 튄다. `@property`의 `initial-value`가 이때
  본래 기본값으로 되돌려 준다. **이 블록을 지우지 않는다.**

## 표제 위계

| 요소 | 사양 |
|---|---|
| `.kicker` 용도표기 | 10.5px / 500 / 자간 0.34em / `--muted` / 가운데 |
| `h1` | 25px / 700 / 줄간 1.34 / 가운데 |
| `.rule86` | 제목 아래 폭 86px, 2px 실선 `--ink-strong` |
| `h2` | 17px / 700 / 전폭 밑줄 1px `--rule-strong` / 위 26px |
| `h3` | 15px / 700 / 위 20px |

## 개조식 기호 위계

내어쓰기(hanging indent)로 기호와 본문을 정렬한다.

| 클래스 | 기호 | 들여쓰기 | 크기 | 색 |
|---|---|---|---|---|
| `.o` | `○` 설명내용 | 1.15em | 본문 | `--ink` |
| `.k` | `가. 나. 다.` 하위항목 | 2.45em | 본문 | `--ink` |
| `.t` | `▶` 부연 | 2.45em | 0.94배 | `--ink-mid` |
| `.n` | `※` 참고·출처 | 2.45em | 0.88배 | `--muted` |
| `.r` | `⇨` 결과·도출 | 1.3em | 본문 / **700** | `--ink-strong` |
| `.lead` | 번호 문단 (기안 본문) | 없음 | 본문 | `--ink` |

`▢`는 쓰지 않는다 (글리프가 없어 `□`로 폴백된다).

## 블록

- `.band` 요약밴드 — 전폭, `--band` 배경, 상하선만, 좌우 테두리 없음
- `.quote` 인용·내부메모 — 밴드와 같은 틀, 0.92배, `--ink-mid`
- `table` — 라벨 열 `th`에 `--band` 음영, 실선 격자 0.8px `--rule`, 수치는 `td.num`으로 우측정렬(텍스트에는 쓰지 않는다), `thead th`는 가운데
- `.foot` 푸터 — `.page` 안 맨 아래. 좌측 문서명 / 우측 `1 / N` 쪽 번호, 9.5px, 상단 헤어라인

## 고칠 때 지킬 것

- **`@page` 를 직접 쓰지 않는다** — `doc-page.js` 가 소유한다. 「용지」 참조
- **`size="a4"` 를 지우지 않는다** — 화면 쪽 카드와 인쇄 용지를 이 한 속성이 정한다
- **`<style>` 맨 위의 `@property --body-fs` / `--body-lh` 블록을 지우지 않는다** — 「서체」 참조
- **`.page` 의 padding(19/20/14mm)을 임의로 바꾸지 않는다** — 「한 쪽에 들어가는 분량」 표가 이 값에서 나온다
- **쪽 방식(고정 쪽 ↔ 흐름 문서)을 바꾸는 변경은 먼저 사용자에게 묻는다** — 쪽 번호·화면 쪽 카드·A4 고정이 한 덩어리로 딸려오므로 작은 선택이 전면 뒤집기가 된다. 2026-09-04·09-07 에 두 번 뒤집혔다
- 푸터를 `slot="footer"` 로 옮기지 않는다 — 흐름 문서와 혼용이 금지되어 있다
- 링크 외 유채색을 넣지 않는다
- 색·크기를 인라인으로 새로 쓰지 말고 `var(--토큰)`을 쓴다

## 파일

- `sync/fit.sh` — **채움을 맞추고 합격·불합격을 찍는다**
- `sync/measure.sh` — 쪽별 채움률
- `sync/orphan.sh` — 외톨이 줄 검사
- `sync/topdf.sh` — 확인용 PDF(아무것도 주입하지 않는다 — 서식만으로 A4 가 나오는지 함께 본다)
- `sync/fontcache.sh` — 측정용 글꼴 내려받기
- `styles.css` — 토큰 시트. `.dc.html`의 `:root`와 같은 값이며, 값을 바꿀 때 두 곳을 함께 고친다
- `templates/report.dc.html` — 보고서 뼈대 (1쪽). Claude Design 에는 `templates/report/report.dc.html` 로 올라간다
- `runtime/doc-page.js`·`support.js` — 지면 런타임 (Claude Design 제공, 수정 금지)

## 관련 스킬

- `doc-design` — 이 디자인 시스템 (설치 위치 `~/.claude/skills/doc-design/`)
- `doc-style` — 어법·종결어미·문서 골격. 8절이 이 시각 디자인의 출처다.
